# Telegram Bot (APScheduler used directly; job-queue extra not needed)
python-telegram-bot==20.7

# OpenAI API (2.x required for GPT-5 models)
openai>=2.37.0

# RAG and Embeddings (OpenAI embeddings by default — no PyTorch)
chromadb==0.4.22
numpy<2.0  # Pin to 1.x for ChromaDB compatibility

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0

# System Monitoring
psutil==5.9.8

# Docker (7.1.0+ includes fix for requests 2.32+ compatibility)
docker>=7.1.0

# Database
aiosqlite==0.19.0

# Scheduling
APScheduler==3.10.4

# Environment Variables
python-dotenv==1.0.1

# HTTP Requests
requests>=2.32.5
aiohttp>=3.13.4

telegramify-markdown>=1.1.5,<2

# Optional: Ollama (local fallback)
# No package needed, uses HTTP requests

# Optional offline embeddings (high RAM): pip install -r requirements-local-embeddings.txt
